Documentation:

This is a utility program that allows user to select multiple files/folders, apply filter and convert the given files from 
source encoding to target encoding. Under "Detect Encoding" tab, it is possible to find out encoding of source files 
by testing a sample against chosen encoding sets. Additionally, under "File Handling" tab, user can rename, change 
extension or search file content of given files.